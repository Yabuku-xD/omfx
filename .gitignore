# Build outputs
zig-out/
.zig-cache/

# Local research / scratch
.research-cache/
*.o
.DS_Store
Thumbs.db

# Runtime state omfx writes into whatever workspace it runs in.
.omfx/

# Bench outputs, regenerated by docs/research/bench-*.py.
archive/research/.bench-*.json

# Secrets and local env (never commit)
.env
.env.*
!.env.example

# Editor / agent local state
.cursor/
.claude/settings.local.json
*.swp
*~

# Local-only archives and backups
git-backup-omfx.tar.gz
omfx-*.tar.gz
SHA256SUMS
